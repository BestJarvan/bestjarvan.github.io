script(src=`${url_for(theme.js)}/utils.js`)
script(src=`${url_for(theme.js)}/stun-boot.js`)
script(src=`${url_for(theme.js)}/scroll.js`)
script(src=`${url_for(theme.js)}/header.js`)

if theme.sidebar.enable
  script(src=`${url_for(theme.js)}/sidebar.js`)

if theme.local_search.enable
  script(type="application/json" src=`${config.root}${config.search.path}`)
